name: Test Gutenberg API (RapidAPI)

on:
  workflow_dispatch:

jobs:
  test-gutenberg:
    runs-on: ubuntu-latest
    steps:
      - name: Test RapidAPI - Books endpoint
        run: |
          echo "=== Books (q=popular, page_size=20) ==="
          curl -s -w "\nHTTP_STATUS:%{http_code}\n" \
            "https://project-gutenberg-free-books-api1.p.rapidapi.com/books?q=popular&page_size=20&page=1" \
            -H "X-RapidAPI-Key: ${{ secrets.GUTENBERG_API_KEY }}" \
            -H "X-RapidAPI-Host: project-gutenberg-free-books-api1.p.rapidapi.com" \
            --max-time 10
