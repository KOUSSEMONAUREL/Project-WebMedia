---
import GridPage from '../components/GridPage.astro';
import { getMediaByType } from '../lib/api';

const res = await getMediaByType('film');
const films = res.data || [];
---

<GridPage title="Films" items={films} pageTitle="Films" />
