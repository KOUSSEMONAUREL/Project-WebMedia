---
import Layout from '../layouts/Layout.astro';
import { getMediaByType, type Media } from '../lib/api';

let series: Media[] = [];
try {
  const res = await getMediaByType('serie');
  series = res.data || [];
} catch {}
---

<Layout title="Séries">
  <div class="relative overflow-hidden">
    <!-- Hero section -->
    <div class="relative pt-20 pb-16 md:pt-28 md:pb-20 bg-gradient-to-b from-purple-950/60 via-background to-background">
      <div class="absolute inset-0 overflow-hidden pointer-events-none">
        <div class="absolute -top-40 -left-40 w-96 h-96 bg-purple-500/10 rounded-full blur-3xl" />
        <div class="absolute -bottom-20 -right-20 w-72 h-72 bg-primary/5 rounded-full blur-3xl" />
      </div>
      <div class="container mx-auto px-6 relative">
        <div class="flex flex-col md:flex-row items-center gap-8 md:gap-16">
          <div class="flex-shrink-0">
            <div class="w-28 h-28 md:w-36 md:h-36 rounded-2xl bg-gradient-to-br from-purple-500 to-purple-700 flex items-center justify-center shadow-2xl shadow-purple-500/30">
              <svg class="w-14 h-14 md:w-20 md:h-20 text-white" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="1.5" stroke-linecap="round" stroke-linejoin="round">
                <rect x="2" y="7" width="20" height="15" rx="2" ry="2"/>
                <polyline points="17 2 12 7 7 2"/>
              </svg>
            </div>
          </div>
          <div class="text-center md:text-left">
            <h1 class="text-5xl md:text-7xl lg:text-8xl font-display font-bold text-white leading-tight">
              <span class="text-purple-400 italic">SÉRIES</span>
            </h1>
            <p class="mt-3 text-lg md:text-xl text-muted-foreground max-w-xl font-light">
              Des histoires qui durent. Plongez dans des univers captivants, saison après saison.
            </p>
            <div class="mt-6 flex flex-wrap gap-3 justify-center md:justify-start">
              <span class="px-4 py-1.5 bg-purple-500/20 text-purple-300 rounded-full text-sm border border-purple-500/30">Drame</span>
              <span class="px-4 py-1.5 bg-purple-500/20 text-purple-300 rounded-full text-sm border border-purple-500/30">Comédie</span>
              <span class="px-4 py-1.5 bg-purple-500/20 text-purple-300 rounded-full text-sm border border-purple-500/30">Crime</span>
              <span class="px-4 py-1.5 bg-purple-500/20 text-purple-300 rounded-full text-sm border border-purple-500/30">Science-Fiction</span>
            </div>
          </div>
        </div>
      </div>
    </div>

    <!-- Grid section avec style série -->
    <div class="container mx-auto px-6 pb-16">
      <div class="border-l-4 border-purple-500 pl-4 mb-10">
        <p class="text-sm text-purple-400 font-medium uppercase tracking-widest">Catalogue</p>
        <h2 class="text-2xl font-display font-semibold text-white">Toutes les séries</h2>
      </div>
      <div class="grid grid-cols-2 sm:grid-cols-3 md:grid-cols-4 lg:grid-cols-5 xl:grid-cols-6 gap-6 md:gap-8">
        {series.length > 0 ? series.map((m) => (
          <a href={`/${m.type}/${m.slug}`} class="group flex flex-col gap-3 transition-all duration-500 hover:-translate-y-2">
            <div class="relative aspect-[2/3] w-full overflow-hidden rounded-xl shadow-lg border border-white/5 group-hover:shadow-purple-500/20 group-hover:shadow-2xl transition-all duration-500">
              <img src={m.posterUrl} alt={m.title} class="h-full w-full object-cover transition-transform duration-700 group-hover:scale-105" loading="lazy" onerror="this.src='https://via.placeholder.com/300x450?text=?'" />
              <div class="absolute top-2 right-2 bg-black/60 backdrop-blur px-2 py-0.5 rounded text-yellow-500 text-xs font-bold flex items-center gap-1">
                <svg class="w-3 h-3 fill-current" viewBox="0 0 24 24"><polygon points="12 2 15.09 8.26 22 9.27 17 14.14 18.18 21.02 12 17.77 5.82 21.02 7 14.14 2 9.27 8.91 8.26 12 2"/></svg>
                {m.rating}
              </div>
            </div>
            <h3 class="font-display font-semibold text-base text-foreground group-hover:text-purple-400 transition-colors line-clamp-2">{m.title}</h3>
            <p class="text-xs text-muted-foreground">{m.year}</p>
          </a>
        )) : (
          <div class="col-span-full py-20 text-center text-muted-foreground">Aucune série trouvée.</div>
        )}
      </div>
    </div>
  </div>
</Layout>
