---
import GridPage from '../components/GridPage.astro';
import { getMediaByType } from '../lib/api';

const res = await getMediaByType('serie');
const series = res.data || [];
---

<GridPage title="Series" items={series} pageTitle="Series" />
