---
import GridPage from '../components/GridPage.astro';
import { getMediaByType } from '../lib/api';

const res = await getMediaByType('book');
const books = res.data || [];
---

<GridPage title="Livres" items={books} pageTitle="Livres" />
