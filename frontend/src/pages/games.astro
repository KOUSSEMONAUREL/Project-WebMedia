---
import GridPage from '../components/GridPage.astro';
import { getMediaByType } from '../lib/api';

const res = await getMediaByType('jeu');
const games = res.data || [];
---

<GridPage title="Jeux Video" items={games} pageTitle="Jeux Video" />
