---
import GridPage from '../components/GridPage.astro';
import { getMediaByType } from '../lib/api';

const res = await getMediaByType('anime');
const animes = res.data || [];
---

<GridPage title="Animes" items={animes} pageTitle="Animes" />
